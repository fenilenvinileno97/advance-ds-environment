# conda env create --file environment.yml
name: vortex_energy
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.11
  - pip:
    - pyhere
    